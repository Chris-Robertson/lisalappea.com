<% @random_category_items.each do |item| %>
  <div class="card mb-2">
    <%# display a random photo for the chosen item of each category %>
    <%= image_tag("#{item.photos[rand(item.photos.count)].path}") %>
    <%= item.category %>
  </div>
<% end %>
